{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm --prefix=.. install && npm --prefix=.. run build --workspace=packages/shared && npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
